# General Knowledge Bank

**Status: work in progress**

This is the **bank** shelf: curated survival-style articles and material that
feed the General Knowledge Library. Content here is still being shaped; treat
paths and filenames as provisional until a release note says otherwise.

## Objectives

- Grow the bank into a stable, themed set of articles aligned with
  `architecture/16_general_knowledge_library.md` (boundaries, uCode spine,
  compatible devices).
- **uDOS Ubuntu** — finish the command-centre so that in a **specific dev
  mode** the host can **clean, organise, expand, and enhance** the bank (e.g.
  linting, indexing, deduping, or batch edits against this tree) without
  hand-editing alone. Until that mode exists, changes land via normal PRs into
  `uDOS-docs` as today.

## Parent

- `../README.md` — library index.
