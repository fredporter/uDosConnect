# Config

`config/` is the checked-in configuration lane for docs publishing and
structure policy.

Current state:

- no separate docs tooling config is required yet
- repo validation is shell-based and lives in `scripts/`

Planned use of this root:

- docs metadata manifests
- publishing or navigation config once the docs surface grows
