# `uDOS-docs` Dev Workspace

Use `@dev` only for compact repo planning and current docs-repo notes.

Use this workspace for:

- repo-specific roadmap items in `@dev/roadmap.md`
- repo-specific requests in `@dev/requests/` as a short active index
- repo-specific submissions in `@dev/submissions/` as a compact completed
  summary
- repo-specific notes in `@dev/notes/`
- repo-specific round records in `@dev/rounds/` only while a live round is open

Family-wide planning, cross-repo routing, and release coordination stay in
`uDOS-dev/@dev`.
