# v2.0.7 Rounds

Status: complete
Updated: 2026-03-21

## Purpose

Stand up the first uHOME-server local streaming channel lane — source adapters,
session controller, and local stream gateway — so household clients can consume
always-on channels across the home network.

## Round A: uHOME-server Streaming Channel Ingestion

Status: completed
Owner: uHOME-server
Binder: #binder/uhome-server-v2-0-7-streaming-channels

Focus:

- define channel source adapters for channel.rewind.mtv and channel.rewind.cartoons
- implement session controller: join, create, sync, resume, move
- implement local stream gateway: web-passthrough and proxy-assisted modes
- expose REST API routes for channel list, session state, and session actions
- add test coverage for service and route layers

Working tags:

- @dev/uhome-streaming

Breakdown:

1. channel source adapter dataclass and two concrete adapters (mtv, cartoons)
2. channel session controller with per-room session registry
3. local stream gateway with passthrough and proxy-assisted modes
4. `channel_service.py` public service combining adapter + session + gateway
5. `channels.py` route module wired into app.py
6. tests: test_channel_service.py and test_channel_routes.py

References:

- @dev/triage/complete/2026-03-17-uhome-streaming-empire-triage.md
- @dev/requests/binder-uhome-server-v2-0-7-streaming-channels.md
- @dev/inbox/briefs/uhome_v_2_streaming_brief.md

Exit criteria:

- channel.rewind.mtv and channel.rewind.cartoons adapters resolve metadata
- session controller handles all lifecycle operations for multiple concurrent rooms
- gateway returns correct mode descriptor for passthrough and proxy-assisted
- REST routes: GET /api/channels, GET /api/channels/{id}, POST /api/channels/sessions,
  GET /api/channels/sessions/{session_id}, POST /api/channels/sessions/{session_id}/move
- all uHOME-server tests pass

## Round B: validation and promotion

Status: completed
Owner: uDOS-dev
Binder: #binder/dev-v2-0-7-release-pass

Outputs:

- uHOME-server full test suite: 189 passed (27 channel tests)
- validation report: @dev/notes/reports/roadmap-status-2026-03-21-000224.md
- submission: @dev/submissions/submission-dev-v2-0-7-release-pass-round-b-complete.md
- binder lifecycle closed; staged for promotion
