# v2.0.3 Rounds

Status: completed  
Updated: 2026-03-15

## Purpose

Break out the active `v2.0.3` roadmap work into round-specific operating notes.

## Round A: Integration Contract Hardening

Status: completed  
Owner: `uDOS-core`  
Binder: `#binder/core-v2-0-3-integration-contracts`

Focus:

- harden cross-repo contracts before widening integrations
- lock the family config, local-state, seed-store, and Wizard secret split
- activate `uDOS-grid` as the canonical spatial identity lane
- keep active consumers aligned to Core contracts and Wizard boundaries

Core references:

- `uDOS-dev/docs/v2.0.3-family-config-state-alignment.md`
- `uDOS-dev/@dev/notes/roadmap/v2.0.3-seed-config-inventory.md`
- `uDOS-dev/docs/repo-family-map.md`
- `uDOS-dev/@dev/notes/roadmap/v3-feed.md`

Working tags:

- `@dev/family-env-contract`
- `@dev/family-user-vars`
- `@dev/family-seed-store`
- `@dev/wizard-secret-store`
- `@dev/grid-spatial-activation`
- `@dev/grid-seed-layer-data`
- `@dev/grid-core-support`

Breakdown:

1. contract ownership confirmation
2. shared config and local-state vocabulary lock
3. seed-store placement and scope rules
4. Wizard secret-store boundary confirmation
5. spatial identity lane activation
6. downstream consumer alignment notes

Future-round rule:

- keep `uDOS-grid` separate for the current dev round
- use `@dev/grid-core-support` as the peg for any later Core-supported spatial
  vocabulary or neutral runtime contract lift

Exit criteria:

- config/state/secret ownership is centralized and non-overlapping
- downstream repos can point to one family alignment doc
- `v2.0.3` Round B can start without reopening the same ownership split

## Round B: Sync Bridges And Product Alignment

Status: completed  
Owner: `family`  
Binder: `#binder/family-v2-0-3-sync-alignment`

Focus:

- end-to-end orchestration and sync alignment
- product consumption of the `v2.0.3` contract set
- config/state visibility across shell, Wizard, server, empire, and app lanes

Working tags:

- `@dev/family-seed-store`
- `@dev/family-config-surfaces`
- `@dev/ucode-workflow-actions`
- `@dev/ucode-assist-adapters`
- `@dev/grid-shell-consumption`
- `@dev/grid-wizard-consumption`
- `@dev/grid-gameplay-consumption`
- `@dev/grid-seed-review`

Breakdown:

1. align variable names and runtime-root expectations
2. inventory repo-local seed stores and starter datasets
3. surface Wizard config and state visibility to consumers
4. confirm sync and handoff paths across Wizard, `uHOME-server`, and
   `uDOS-empire`

Current review note:

- use `@dev/grid-seed-review` for seed-data audit, refinement, and starter
  expansion work before broader spatial promotion

Central reference:

- `uDOS-dev/@dev/notes/roadmap/v2.0.3-seed-config-inventory.md`
- `uDOS-dev/@dev/notes/roadmap/v2.0.3-defaults-vs-examples-rule.md`
- `uDOS-dev/docs/v2.0.3-grid-consumption-alignment.md`

Next binder:

- `#binder/family-v2-0-3-grid-consumption`

Exit criteria:

- first connected cross-repo product paths are legible and testable
- the family uses the same config/state language across active repos

## Round C: Validation And Promotion

Status: in-progress  
Owner: `uDOS-dev`  
Binder: `#binder/dev-v2-0-3-release-pass`

Focus:

- repo validation
- promotion notes
- release-scope and readiness decisions for the integrated `v2.0.3` round
- `@dev` agent-assist instructions: capture the binder workflow, `@dev/` directory
  convention, family topology, and round/promote/close cycle as a VS Code
  `.instructions.md` so any agent can resume dev work without re-briefing

Working tags:

- `@dev/dev-agent-assist`

Reference:

- `@dev/notes/roadmap/dev-agent-assist.md`
- `.github/instructions/dev-workflow.instructions.md`

Exit criteria:

- full family test suite passing across 8 repos (363 tests) ✓
- v2.0.3 promotion notes written and committed ✓
- `.github/instructions/dev-workflow.instructions.md` created and committed ✓
- `#binder/dev-v2-0-3-release-pass` open in `@dev/requests/` ✓
- promotion PRs opened for each active repo (`develop` → `main`) — pending
- release tags cut from `main` — pending
- round status updated to `completed` in ledger — pending
