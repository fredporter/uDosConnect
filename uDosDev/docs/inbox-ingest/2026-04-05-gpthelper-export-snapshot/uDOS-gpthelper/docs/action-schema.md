# Export Action Schema

Use `actions/export-openapi.json` in the GPT Builder → Actions panel.

## Notes
- replace the placeholder server URL
- use the ngrok/public URL during early testing
- later replace with your stable `uDOS-host` helper domain
