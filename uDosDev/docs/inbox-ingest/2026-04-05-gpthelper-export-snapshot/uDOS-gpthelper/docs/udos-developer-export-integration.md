# uDOS Developer Export Integration

Future use:
- export code binders
- export IDE handoff packs

Suggested rule:
When EXPORT is used and the export action is available, package the generated code-binder files into a ZIP and return the download link.
