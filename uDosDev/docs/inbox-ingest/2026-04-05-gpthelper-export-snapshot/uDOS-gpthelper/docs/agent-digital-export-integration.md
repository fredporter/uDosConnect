# Agent Digital Export Integration

Append this to Agent Digital instructions:

FILE GENERATION RULE: When COMPILE is used, generate the final files and call the export action to package them into a ZIP. Return the download link from the action result. Do not claim ZIP export is impossible if the export action is available.
