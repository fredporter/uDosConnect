# Archived: uDOS family v2 Cursor workspaces

These **`.code-workspace`** files were the numbered **`cursor-01` … `cursor-09`**, **completion rounds 00–04**, and **`uDOS-v2-public`** multi-root layouts from the pre–**uDOS-v3** era. They remain usable for historical reference or parity archaeology.

**Current work:** use **`uDOS-v3.code-workspace`** at the `uDOS-family` checkout root (v3 monorepo + governance + themes + UniversalSurfaceXD). See **`../README.md`**.

## Path note

Files were moved from `uDOS-dev/workspaces/` into this folder; folder `path` entries were adjusted so repo roots still resolve correctly relative to each JSON file.

## Contents

| File | Role (historical) |
| --- | --- |
| `uDOS-v2-public.code-workspace` | Broad family tree (orientation) |
| `cursor-01-runtime-spine.code-workspace` … `cursor-09-classic-modern-mvp.code-workspace` | Numbered execution lanes |
| `completion-round-00-v2-6-spine-parity.code-workspace` … `completion-round-04-gui.code-workspace` | Completion / spine rounds |

Supporting markdown (round narratives, local stack notes) still lives next to this archive under **`../../`** (e.g. `completion-rounds-and-local-stack.md`).
