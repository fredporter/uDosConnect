# Policy Templates

Checked-in non-secret policy templates for Ubuntu host runtime actions.

Use this lane for:

- outbound Git and GitHub approval defaults
- audit posture defaults
- operator-controlled safety gates for high-impact host actions

These files are templates only. Host-installed policy may be rendered into
`/etc/udos/` or equivalent secure runtime config paths.
