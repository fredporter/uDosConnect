# Source Lane

Reserved for repo-local helper tooling as implementation deepens.

The first runnable service scaffold currently lives in `scripts/`.

When real daemon/service implementations land, they may graduate into `src/`
behind the stable `udos-*.sh` wrapper entrypoints.
