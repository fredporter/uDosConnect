# Scripts

- `bootstrap.sh` prepares the local adapter workspace
- `clone-deerflow.sh` clones the pinned upstream dependency
- `update-deerflow.sh` refreshes the pinned upstream dependency
- `run-deerflow-checks.sh` validates the local plugin lane surfaces
