# Config

Workspace configuration remains intentionally thin in the first activation
slice.

Current configuration direction:

- browser app configuration stays local to `apps/web/`
- compile execution configuration is Wizard-owned
- publishing configuration is Empire-owned
- canonical binder, task, and spatial semantics stay outside this repo
