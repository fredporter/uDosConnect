# Browser Adapter

Maps tokens and primitives into browser GUI output for Svelte or HTML-based
uDOS surfaces.

Implemented surface:

- `index.mjs` renders a browser screen model with prose-compatible markup and
  CSS variable output.
