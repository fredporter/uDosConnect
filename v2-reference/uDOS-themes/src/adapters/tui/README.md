# TUI Adapter

Maps tokens and primitives into text-first TUI output and guided full-viewport
flows.

Implemented surface:

- `index.mjs` renders a viewport-first TUI frame with progress, wrapped body
  text, and action rows.
