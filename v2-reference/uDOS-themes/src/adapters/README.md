# Adapters

`adapters/` maps theme contracts into renderable output for each surface.

Required lanes:

- `browser/`
- `thinui/`
- `tui/`
- `workflow/`
- `publish/`
- `forms/`

Working implementation files now live beside the contract docs in each lane.
