import type { ThinUiFontDefinition } from "../contracts/types";

export const petmeFont: ThinUiFontDefinition = {
  id: "petme-default",
  label: "Petme Default",
  family: "Petme64",
  source: "optional",
  fallbackFamily: "monospace",
  monospace: true,
};
