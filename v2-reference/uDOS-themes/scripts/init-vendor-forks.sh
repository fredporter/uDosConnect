#!/usr/bin/env bash
# v2-reference archive: vendor/forks are vendored file trees (not git submodules).
set -eu
echo "v2-reference/uDOS-themes: vendor/forks are already materialized; no submodule init."
exit 0
