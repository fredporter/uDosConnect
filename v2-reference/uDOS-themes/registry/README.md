# Registry

`registry/` stores the machine-readable index for themes, adapters, and skins.

These files are the handoff-safe entrypoint for consumers that need to inspect
what `uDOS-themes` currently exposes.

Human-readable rollout and consumption order: **`docs/adapter-skin-registry-plan.md`**.
