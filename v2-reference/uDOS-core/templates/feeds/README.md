# Feed Templates

These templates provide stable output skeletons for rendering feed items into
common syndication formats.

They are reference templates, not a mandatory runtime implementation.

Supported formats:

- `rss.xml`
- `atom.xml`
