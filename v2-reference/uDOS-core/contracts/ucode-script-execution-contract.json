{
  "version": "v2.2",
  "owner": "uDOS-core",
  "summary": "Reference execution contract for markdown-backed uCODE scripts in the Core runtime.",
  "entrypoints": [
    "RUN <path-to-script.md>",
    "SCRIPT RUN <path-to-script.md>"
  ],
  "frontmatter": {
    "required": ["id", "type", "version", "runtime"],
    "type": "script",
    "version": 2,
    "runtime": ["tui", "web"]
  },
  "execution": {
    "block_language": "ucode",
    "ordering": "Blocks execute in document order; statements execute line-by-line within each block.",
    "failure_mode": "Stop on first failed statement and return executed statements plus the failing command.",
    "network_policy": "Offline-safe only. Network behavior remains out of scope for Core."
  },
  "verbs": [
    "SET",
    "STATUS",
    "WORKFLOW RUN",
    "DRAW BLOCK",
    "DRAW PAT TEXT",
    "RUN",
    "SCRIPT RUN"
  ]
}
