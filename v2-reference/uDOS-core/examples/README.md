# uDOS-core Examples

Stable machine-readable examples for core-owned contracts and public reference
surfaces live here.

Current example groups:

- `logs-feeds-spool/` for event records, feed items, feed creation requests, and
  spool payloads

Examples should stay compact, deterministic, and valid against the canonical
schemas in `schemas/`.
