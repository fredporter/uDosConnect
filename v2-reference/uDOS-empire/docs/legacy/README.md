# Empire Legacy Docs

`docs/legacy/` holds superseded Empire documents from the earlier sync,
provider-lane, and broad operations-container framing.

These records are kept for migration context and compatibility only.

Use the active docs in the parent `docs/` folder for the current public
direction:

- `wordpress-plugin-architecture.md`
- `wordpress-plugin-data-model.md`
- `architecture.md`
- `boundary.md`
- `contact-and-crm-model.md`

Rule:

- do not use legacy docs as the default product identity
- only consult them when migrating older Google, HubSpot, webhook, or
  operations-container materials
