# Superseded: Provider Template Patterns

This earlier provider-template note has moved to
`docs/legacy/provider-template-patterns.md`.

Use it only for migration context around older Google, HubSpot, and webhook
assets.
