# WordPress Contact Import

This starter pack previews a local WordPress CRM import batch for Empire's new
plugin direction.

Use it to inspect:

- import batch naming
- workspace linkage
- contact-note append lines
- dry-run review text before committing the import in WordPress

This pack is intentionally local-first. It does not produce a HubSpot sync
package or webhook job.
