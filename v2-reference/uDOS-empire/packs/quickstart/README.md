# Quickstart

`quickstart` is a starter orientation pack for first-time Ubuntu host
operators using `uDOS-empire` packs through the active host runtime seam.

It demonstrates:

- preview-first understanding of inputs and outputs
- a minimal reviewed workflow hosted by the Ubuntu command-centre runtime
- pack structure that users can inspect before moving to richer packs
