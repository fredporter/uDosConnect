The demo binder JSON is maintained in **`uDOS-thinui`**:

- `uDOS-thinui/src/workspace/demo-binder.json` (bundled default)
- `uDOS-thinui/examples/demo-binder.json` (copy for repo layout)

Do not add a second canonical JSON here; update ThinUI only.
