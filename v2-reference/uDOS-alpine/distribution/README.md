# Distribution

APK repos, remaster flows, and image outputs.

Release note:

- this directory defines packaging and image intent
- release attachment remains notes-first until Alpine artifact generation is a
  stable, documented public lane
