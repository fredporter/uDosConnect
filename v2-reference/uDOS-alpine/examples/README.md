# Examples

`examples/` is the operator-facing walkthrough lane for `uDOS-alpine`.

Current example lanes:

- `basic-alpine-build.md` for the smallest Alpine packaging workflow

Phase 1 rule:

- keep examples aligned to supported Alpine profiles
- route generic deployment behavior back to Sonic where appropriate
