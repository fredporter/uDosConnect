# Scripts

`scripts/` is the checked-in validation lane for `uDOS-gameplay`.

Current script surfaces include:

- `run-gameplay-checks.sh` for repo activation validation

Boundary rule:

- keep lightweight gameplay checks here
- keep runtime execution semantics outside this repo
