# Config

`config/` is the checked-in configuration lane for `uDOS-gameplay`.

Current state:

- no separate gameplay config files are required yet
- repo validation is shell and Python standard-library based

Planned use of this root:

- gameplay profile defaults
- scenario configuration examples
