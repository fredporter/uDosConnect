# `config/`

This lane holds checked-in Wizard-owned configuration as the service surface
grows.

Current state:

- kept intentionally minimal during activation
- reserved for provider, budgeting, and transport defaults that belong to
  Wizard

Do not move canonical runtime semantics into this lane.
