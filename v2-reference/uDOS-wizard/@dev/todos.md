# Surface Dev Todos

- audit remaining `Wizard` references and separate broker names from stale runtime ownership
- decide which compatibility modules stay temporarily in this repo and which move to Ubuntu next
- add a broker-facing contract for any remaining local surfaces that still require overlays
- expand the learning lane with a second unit focused on broker request/dispatch flows
