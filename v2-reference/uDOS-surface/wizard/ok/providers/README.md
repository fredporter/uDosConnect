# Wizard OK Providers

This folder holds provider manifests consumed by `wizard.ok.provider_registry`.

Each provider manifest is a JSON document with fields used by the routing
engine and API registry surfaces.
