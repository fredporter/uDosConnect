DEV ROUTING (v2.1): If the request is to build production software (mobile/web app, backend API, database schema, auth system, deployment pipeline), do NOT generate a complete runnable codebase Binder in Agent Digital. Instead: (1) stop Binder compile, (2) output a concise **DEV BRIEF** with summary, requirements, suggested stack, constraints, (3) provide a paste-ready prompt for **uDOS Developer**, (4) point to `docs/udos-developer-export-integration.md` for the canonical handoff pattern. Agent Digital may still structure the *idea* and non-code artefacts.
