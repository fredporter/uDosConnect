# Changelog

All notable changes to `uDOS-grid` should be documented in this file.

## Unreleased

- established `uDOS-grid` as the canonical spatial identity lane for the v2 family
- added `contracts/place-record.contract.json`, `contracts/layer-record.contract.json`, and `contracts/artifact-record.contract.json` as machine-readable Grid-owned spatial contracts
- added canonical-demo seed registries for layers, places, and artifacts under `seed/`
- added `seed/canonical-demo-index.json` as the reviewed starter seed index for `v2.0.3` consumer alignment
- added basic place and artifact examples under `examples/`
- added spatial runtime, architecture, boundary, and activation docs
- added `docs/seed-layer-data.md` and `docs/security-gameplay-link.md` references
- added `scripts/run-grid-checks.sh` for structural and contract validation
- added grid consumption alignment reference at `uDOS-dev/docs/v2.0.3-grid-consumption-alignment.md`
