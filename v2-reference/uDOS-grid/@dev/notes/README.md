# `@dev/notes`

Keep only current repo-owned spatial notes here.

Use one note per concept. Stable public reference belongs in `/docs`, while
`@dev/notes` is for working material that still informs active implementation.
