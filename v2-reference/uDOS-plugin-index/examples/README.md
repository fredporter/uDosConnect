# Examples

`examples/` is the smallest inspectable contract walkthrough lane for
`uDOS-plugin-index`.

Current example lanes:

- `basic-plugin-manifest.json` for the smallest valid plugin manifest example

Phase 1 rule:

- keep examples stable and schema-aligned
- keep executable plugin behavior in the owning repos
