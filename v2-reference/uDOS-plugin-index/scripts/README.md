# Scripts

`scripts/` is the checked-in validation lane for `uDOS-plugin-index`.

Current script surfaces include:

- `run-plugin-index-checks.sh` for repo activation validation

Boundary rule:

- keep schema and contract checks here
- keep install and execution tooling outside this repo
