# Contracts

`contracts/` is the public contract lane for `uDOS-plugin-index`.

Current contract surfaces include:

- `plugin-manifest.md` for the plain-language manifest contract

Boundary rule:

- keep contract semantics here
- keep runtime loading behavior in the owning runtime repos
